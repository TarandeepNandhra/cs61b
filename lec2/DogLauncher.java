/**
The DogLauncher class will test drive the Dog class
Java goes to Dog.java and finds the makeNoise method.
*/

public class DogLauncher {

    public static void main(String[] args) {
        // Declartion of a Dog variable
        Dog smallDog
        // instantiation of the Dog class as a Dog object
        new Dog(15); // not assigned so would get destroyed / los
        // instantiation and assignment
        bigdog = new Dog(10);
        // Declartion, instantiation and assignment
        Dog d = new Dog(51);
        // Invoking Dog's makeNoise with 51 pounds (on d)
        d.makeNoise();
    }

}
